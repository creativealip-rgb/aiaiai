# Uploaded product / category images are written here by `/api/uploads`.
# Files in subfolders (`products/2026/01/...`) are served statically at `/uploads/...`.
#
# Don't commit the actual uploads — see `.gitignore`. This README keeps the
# directory present in Git so local dev + Docker builds don't have to mkdir.
